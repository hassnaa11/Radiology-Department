<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scan</title>
    <link rel="stylesheet" href="/css/patient_report.css">
</head>

<body>
    <div class="top-side">
        <span>Scan Image</span>
    </div>
    <div class="left">
        <% if (selectedPic) { %>
            <div>
            <img src="/images/<%=scan_id %>/<%=selectedPic %>.png" class="scan-image" alt="Scan Image">
        </div>
        <% } else { %>
            <p>No image available</p>
        <% } %>
        <!-- <% console.log(selectedPic) %>
        <img src="<%= selectedPic %>" class="scan-image"> -->
    </div>
</body>

</html>